[nosetests]

exe=1

with-doctest=1

with-coverage=1
cover-package=doorstop.common,doorstop.core,
    doorstop.cli.main,doorstop.cli.utilities,
    doorstop.gui,doorstop.web
cover-erase=1
cover-min-percentage=100

verbosity=1
logging-level=DEBUG
logging-format=[%(levelname)-8s] %(message)s
